import { OpsAccountsContent } from "./_components/ops-accounts-content";

export const metadata = {
  title: "Comptes | SnapSell",
  description: "Aide à l’accès aux comptes de boutique, réservée au support SnapSell.",
};

export default function OpsAccountsPage() {
  return <OpsAccountsContent />;
}
